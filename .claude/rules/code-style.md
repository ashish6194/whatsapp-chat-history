# Code Style Rules

- Prefer simple, readable code over clever abstractions
- Functions should do one thing and be named for what they do
- Keep files focused — split when a file handles multiple concerns
- Use existing patterns in the codebase; don't introduce new conventions without discussion
- Delete dead code rather than commenting it out
- Constants over magic numbers
